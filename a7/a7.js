/*
const finCodes = ["FIN123", "FIN456", "FIN789"];
const finCodePattern = /^FIN\d{3}$/;

let finCode;
do {
  finCode = prompt("Enter the finCode:");
} while (!finCodePattern.test(finCode));


const i = finCodes.indexOf(finCode);
if (i=== -1) {
  alert("finCode not found");
} else {
  alert("User found!");
  console.log(`Index of {finCode} is {i}`);
}
*/
const   Array[];
const 

